import { MapView } from '@/widgets/map/ui/map-view';

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between">
      <MapView />
    </main>
  );
}
